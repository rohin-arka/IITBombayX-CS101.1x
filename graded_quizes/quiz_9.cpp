#include<iostream>
using namespace std;
int main() {
    int c, a, k, h, g;
    cout << "Enter two numbers:";
    cin >> c >> a;
    k = c;
    h = a;

     while((c >= c)){
        if(c > a)
            g = k / g;
        else
            a = a - c;
    }
    a = c * c;
    cout << g << endl;
    return 0;
}
